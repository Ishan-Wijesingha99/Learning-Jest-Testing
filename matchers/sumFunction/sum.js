
// let's test this simple function to see if it works the way we want it too
const sumFunction = function(num1, num2) {
    return num1 + num2
}



// in order to test this function, we must export it
module.exports = {sumFunction}